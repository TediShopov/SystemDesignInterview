using UnityEngine;

public class Properties : MonoBehaviour
{
    [Range(0, 100)]
    [SerializeField] public float SoundModifier;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
